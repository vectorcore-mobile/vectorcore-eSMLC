# Corrected reconstruction

V2 produced 366 blocks. Reconstruction assigns 001–360 to
`LPP-PDU-Definitions` and 361–366 to `LPP-Broadcast-Definitions`, with zero
unassigned and zero duplicate blocks.
