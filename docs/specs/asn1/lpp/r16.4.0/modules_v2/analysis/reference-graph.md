# Reference graph

652 definition nodes and 971 conservative edges. Edges contain only declared local definitions or declared imports; ASN.1 field labels and enumerated labels are deliberately excluded.
